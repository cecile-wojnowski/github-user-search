import React from "react";
import { render, cleanup } from "@testing-library/react";

afterEach(cleanup);

describe("Testing <Card/> component", () => {
  it("Should render a card with an avatar, id, name and a button", () => {});

  it("Should render a card with a checkbox", () => {});
});
