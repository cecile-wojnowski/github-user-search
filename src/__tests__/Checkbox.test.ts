import React from "react";
import { render, cleanup } from "@testing-library/react";

afterEach(cleanup);

describe("Testing <Checkbox/> component", () => {
  it("Should render a checkbox", () => {});

  it("Should be checked and unchecked on click", () => {});
});
